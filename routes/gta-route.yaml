apiVersion: networking.gloo.solo.io/v2
kind: RouteTable
metadata:
  name: gta-route
  namespace: gh-action-idp
spec:
  hosts:
    - 'gta.demo.glooplatform.com'
  virtualGateways:
    - name: mgmt-north-south-gw-443
      namespace: istio-gateways
      cluster: mgmt
  workloadSelectors: []
  http:
    - name: gta-route
      matchers:
        - uri:
            prefix: /get
      forwardTo:
        destinations:
          - ref:
              name: httpbin
              namespace: gh-action-idp
            port:
              number: 80
